# NasMate PRD 实现状态

本清单以当前代码和自动化测试为准，不把未验证的 UGOS 真机能力视为已完成。

| PRD 区域 | 状态 | 说明 |
| --- | --- | --- |
| 对话与任务中心 | 已实现 | 任务状态、取消、恢复、事件轨迹、持久化状态均已接入。 |
| 文件元数据搜索 | 已实现 | 授权根目录、路径穿越与符号链接防护、分页搜索。 |
| 容量诊断与健康日报 | 已实现 | 只读统计、报告生成、历史查看与下载。 |
| Docker 只读诊断 | 接口已实现 | 生产环境等待经官方文档验证的 UGOS provider；开发模式使用安全 mock。 |
| 备份与恢复验证 | 接口已实现 | 状态检查与恢复演练计划可用；真实恢复执行不自动开放。 |
| 网络素材检索与下载 | 部分实现 | URL 探测、来源/许可证校验、空间检查、审批和任务进度已实现；通用公开网络搜索适配器仍待接入。 |
| 批量整理 Dry Run | 已实现 | 月份、扩展名、项目目录和疑似重复四种只读计划。 |
| 本地知识索引 | 已实现（元数据模式） | 索引状态、异步重建进度、取消、重启中断保护和分页搜索可用；正文/OCR/音视频需显式授权，默认关闭。 |
| 模型与 Harness | 已实现（受控规划） | 工具 schema、策略校验、预算、审批事件和敏感信息脱敏已接入；支持 DeepSeek/OpenAI-compatible 云端及 Ollama/vLLM 本地端点配置。 |
| UGOS 上架交付 | 部分完成 | 官方 `ugcli v1.1.0.25` 已通过项目检查，并在 Debian 12 容器生成 amd64/arm64 nasync `.upk`；开发授权文件已收到，仍需将其改名为 `ugdev.sig` 上传目标 NAS 后完成真机安装、启动和上架验收。 |

## 当前安全边界

- 不提供任意 Shell、`docker exec`、自动删除、自动修改容器或自动修改权限。
- 写入操作必须经过计划和真实 UI 审批；模型输出不能伪造审批。
- 文件操作仅限授权根目录，并拒绝路径穿越和嵌套符号链接逃逸。
- API Key、密码、Cookie 和 Docker 日志敏感字段不会进入会话日志或模型上下文。
